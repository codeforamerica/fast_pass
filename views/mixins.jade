mixin nav-button(section, value)
	if attributes.class
		button(type='button', value='#{value}', data-section='#{section}', class='#{attributes.class}')
			block
	else
		button(type='button', value='#{value}', data-section='#{section}')
			block

mixin nav-back(section)
	if section
		button(type='button', value='back', data-section='#{section}')
			if block
				block
			else
				| Back
	else
		button(type='button', value='back')
			if block
				block
			else
				| Back

mixin nav-next(section)
	if section
		button(type='button', value='next', data-section='#{section}')
			if block
				block
			else
				| Next
	else
		button(type='button', value='next')
			if block
				block
			else
				| Next

mixin radio-button(section)
	p
		button(type='button', data-section='#{section}')
			block

mixin external-link-button(id, link)
	p
		button(type='button', id='external-#{id}', data-external-link='#{link}')
			block

mixin modal-link(header)
	//- Keep a space between link and preceding text
	&nbsp;
	if attributes.title
		a.modal(href='', data-title='#{header}', title='#{attributes.title}')
			block
	else 
		a.modal(href='', data-title='#{header}')
			block


mixin checkbox(id)
	input(type='checkbox', name='additional-business', value='1', id='#{id}')
	label(for='#{id}')
		block

	a.small.help.modal(href='', data-title='#{id}')
	br


mixin loading
	.loading
		img(src='img/loading-lite.gif')


//- DISPLAY SPECIFIC


mixin zone(color, zone_id, zone_name)
	li
		strong
			span(class='clickable #{color} pill')= zone_id
			+modal-link('#{zone_id} #{zone_name} Zone Definition')(title='click to highlight zone on map')= zone_name


